/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'github.com',
      'images.unsplash.com',
      'via.placeholder.com',
    ],
  },
};

export default nextConfig;
